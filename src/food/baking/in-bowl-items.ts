import { CustomItem } from '../../common/items/CustomItem';
import { VkItem } from '../../common/items/VkItem';

// Dough models for bowl
export const DoughBreadInBowl = new CustomItem({
  type: VkItem.HIDDEN,
  id: 16,
});
export const DoughBreadRisenInBowl = new CustomItem({
  type: VkItem.HIDDEN,
  id: 17,
});
export const DoughPieInBowl = new CustomItem({
  type: VkItem.HIDDEN,
  id: 19,
});
export const DoughCookieInBowl = new CustomItem({
  type: VkItem.HIDDEN,
  id: 18,
});
